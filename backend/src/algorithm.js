/**
 * @import { SolvedNestedID, WithoutID, User, Course, Preferences, TimeTable } from '../../common/models'
 */

/**
 * @param {User} user
 * @param {SolvedNestedID<Course>[]} courses
 * @param {Preferences} preferences
 * @return {WithoutID<TimeTable>[]}
 */
export function generateTimetable(user, courses, preferences) {
    
}